<!DOCTYPE html>

<% if(message.success) { %>
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet" type="text/css"> -->
    <div class="alert alert-success" role="alert">
        <%= message.success %>
    </div>
<% } %>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>  <img src="<%= locals.logo %>" alt="Company Logo" > 
</head>
<body>

    <h1>Company Information (<%= locals.name_ticker %>)</h1>
 
    <form action="/company_upd/?_method=PUT" method="POST">
        <label>
            Company Name:<input type="text" name="name_company" value="<%= locals.name_company %>" size="50" placeholder="No company name available"/><br>
            Company PE:<input type="text" name="PE" value="<%= locals.PE %>" size="50" placeholder="No PE available"/><br>
            Ticker Symbol(read only):<input type="text" name="name_ticker" value="<%= locals.name_ticker %>" size="50" placeholder="No ticker symbol available!"
            onkeyup="this.value = this.value.toUpperCase()" readonly  /> <br>

            Dividend(in USD):<input type="text" name="dividend" value="<%= locals.dividend %>" size="50" placeholder="No dividend data available"/><br>

            Current Stock Price:<input type="text" name="stock_price" value="<%= locals.stock_price %>" size="50" placeholder="Stock price unavailable!" readonly/>
            <input type="hidden" name="_method" value="PUT"> <br>

            <button type="submit" value="PUT">Update</button>

        </label>
      </form>
<br>

<form action="/company_del/?_method=DELETE" method="POST">
    <label>
        Click Delete to Delete company
        <input type="hidden" name="name_ticker2" value="<%= locals.name_ticker %>" size="50"/><br>
        <input type="hidden" name="_method" value="DELETE">

    </label>
    <button type="submit" value="DELETE">Delete</button>
  </form>

  <br>
  <a href="/company">Back to company list </a>
</body>
</html>